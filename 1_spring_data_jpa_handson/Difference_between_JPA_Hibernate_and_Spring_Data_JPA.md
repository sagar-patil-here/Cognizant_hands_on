# Difference between JPA, Hibernate and Spring Data JPA

- **JPA**: A specification for accessing, persisting, and managing data.
- **Hibernate**: An implementation (ORM framework) of JPA.
- **Spring Data JPA**: Adds a layer of abstraction on top of JPA providers to reduce boilerplate code.
